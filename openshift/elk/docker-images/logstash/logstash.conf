input {
  http {
  }
  tcp {
    port => 28777
    type => "log"
    tags => ["server-side"]
  }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	  codec => rubydebug
	}
	stdout {}
}
