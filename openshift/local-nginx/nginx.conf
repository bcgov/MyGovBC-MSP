worker_processes  auto;

error_log  /var/log/nginx/error.log;
pid        /var/run/nginx.pid;

events {
    worker_connections  4096;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    server_tokens off;
    underscores_in_headers on;

    # ip filtering
    allow 0.0.0.0/0; deny all;

    # logging rules
    geo $loggable {
        default 1;
        172.51.0.0/16 0;
    }

    # Use a w3c standard log format
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main if=$loggable;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    #real_ip module
    set_real_ip_from 172.51.0.0/16;
    set_real_ip_from 142.34.208.209; set_real_ip_from 142.34.63.248/29;
    real_ip_recursive on;
    real_ip_header    X-Forwarded-For;

    #throttle zones
    limit_req_zone $binary_remote_addr zone=bra1:10m rate=1r/s;
    limit_req_zone $binary_remote_addr zone=bra3:10m rate=3r/s;
    limit_req_zone $binary_remote_addr zone=bra5:10m rate=5r/s;
    limit_req_zone $binary_remote_addr zone=bra25:10m rate=25r/s;

    #default throttle; not inherited if set in nested level
    # limit_req_zone $binary_remote_addr zone=bra5:10m rate=100r/s;
    # limit_req zone=bra5 burst=100;
    limit_req_zone $binary_remote_addr zone=bra100:10m rate=1400r/s;
    # limit_req zone=bra100 burst=1400;

    client_max_body_size 100m;

    # HTTP Basic rules
    # auth_basic_user_file /tmp/.htpasswd;

    server {
        listen       9999;
        server_name  localhost;

        # add in most common security headers
        add_header Content-Security-Policy "default-src * data: blob: filesystem: 'unsafe-inline' 'unsafe-eval'";
        add_header Strict-Transport-Security "max-age=86400; includeSubDomains";
        add_header X-Content-Type-Options "nosniff";
        add_header X-XSS-Protection 1;
        add_header X-Frame-Options DENY;


        # TODO - VERIFY BUILD PATHS

        # index  index.html index.htm; TODO - UNCOMMENT FOR TEST2

        # serve our angular app here
        location / {
            rewrite ^/msp(/.*)$ $1;
            root   /tmp/app/dist;
            index  index.html index.htm; ## TODO - RECOMMENT FOR TEST2
            try_files $uri /index.html;
            gzip            on;
            gzip_min_length 1000;
            gzip_types      *;

            # Deploy-time configurable
            #
        }

        # location /deam-interim {
        #     # rewrite ^/msp(/.*)$ $1;
        #     root   /tmp/app/dist/deam-interim;
        #     index  index.html index.htm; ## TODO - RECOMMENT FOR TEST2
        #     try_files $uri /index.html;
        #     gzip            on;
        #     gzip_min_length 1000;
        #     gzip_types      *;

        #     # Deploy-time configurable
        #     #
        # }


        # Allows loading of /deam-interim/ to hit homepage
        location /deam-interim/ {
            # rewrite ^/msp(/.*)$ $1; # untested
            # TODO - As Per Jam, try to use "root" instead of Alias here!
            alias /tmp/app/dist/deam-interim/;
            # try_files /tmp/app/dist/deam-interim/$uri /tmp/app/dist/deam-interim/$uri/;
        }

         # Allows loading of /deam-interim/ to hit homepage
        location /deam-interim/msp {
            # rewrite ^/msp(/.*)$ $1; # untested
            # TODO - As Per Jam, try to use "root" instead of Alias here!
            alias /tmp/app/dist/deam-interim/;
            # try_files /tmp/app/dist/deam-interim/$uri /tmp/app/dist/deam-interim/$uri/;
        }

        # Fixes loading of css files/assets, but doesn't fix deep linking / direct to page.
        location /msp/deam-interim/ {
            alias /tmp/app/dist/deam-interim/;
        }

        # TODO - Directly hit http://localhost:8080/deam-interim/msp/account/prepare

        # # VERIFY - FALSE - Allows reloading of http://localhost:9090/deam-interim/msp
        #  location /deam-interim/msp/ {
        #     alias /tmp/app/dist/deam-interim/;
        # }

        # TODO - Can directly hit http://localhost:9090/deam-interim/msp/account/prepare
        # Currently -> redirects to wrong homepage ("/msp/")
        location /deam-interim/msp/account/prepare {
            # root /tmp/app/dist/deam-interim;
            try_files $uri $uri/ /tmp/app/dist/deam-interim/index.html;
            # index index.html index.htm;
            ## hits route but points to wrong index file, and goesback to home page
            alias /tmp/app/dist/deam-interim/;
        }

        # trying http://localhost:9090/msp/deam-interim/account/prepare (note position of "msp")
        # points to correct index but hits homepage, maybe because url (order of msp) differs?
        location /msp/deam-interim/account/prepare {
            # root /tmp/app/dist/deam-interim;
            try_files $uri $uri/ /tmp/app/dist/deam-interim/index.html;
            # index index.html index.htm;
            ## hits route but points to wrong index file, and goesback to home page
            alias /tmp/app/dist/deam-interim/;
        }

        # kitchen sink, trying both http://localhost:9090/msp/deam-interim/msp/account/prepare/
        # works, but points to homepage
        location /msp/deam-interim/msp/account/prepare {
            # root /tmp/app/dist/deam-interim;
            try_files $uri $uri/ /tmp/app/dist/deam-interim/index.html;
            # index index.html index.htm;
            ## hits route but points to wrong index file, and goesback to home page
            alias /tmp/app/dist/deam-interim/;
        }

        # location ^~ /acm/ {
        #     alias /tmp/app/dist/deam-interim;
        #     try_files  $uri $uri/;
        # }



        # location /interim {
            # rewrite ^/msp(/.*)$ $1;
        #     root   /tmp/app/dist/deam-interim; #TODO: Verify
        #     index  index.html index.htm;
        #     try_files $uri /index.html;
        #     gzip            on;
        #     gzip_min_length 1000;
        #     gzip_types      *;
        # }

        # location /interim-no-rewrite {
        #     root   /tmp/app/dist/deam-interim; #TODO: Verify
        #     index  index.html index.htm;
        #     try_files $uri /index.html;
        #     gzip            on;
        #     gzip_min_length 1000;
        #     gzip_types      *;
        # }




        # location /test {
        #     root   /tmp/app/dist/deam-interim; #TODO: Verify
        #     return 201; # Debugg only, don't really want to return status code
        # }

        # # location /testFile {
        # #     root   /tmp/app/dist/deam-interim; #TODO: Verify
        # #     try_files index.html;
        # # }

        # location /testMissing {
        #     root   /tmp/app/dist/deam-interim; #TODO: Verify
        #     try_files index.html =404;
        # }



        location /api {
           return 200;
        }

        # redirect server error pages to the static page /50x.html
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }

        # no serving Dockerfile
        location = /Dockerfile {
            root   /usr/share/nginx/html;
        }

        # For status of ngnix service, OpenShift is configured to call this
        location /nginx_status {
            # Enable Nginx stats
            stub_status on;

            # Only allow access from within OpenShift
            allow 172.51.0.0/16;

            # Other request should be denied
            deny all;

            # No need to log this request, its just noise
            access_log off;
        }
    }
}

